# IDA-Graphy Dependencies
# =============================================================================

# YAML configuration parsing
PyYAML>=6.0

# CSV processing (optional, for validation)
pandas>=1.5.0

# Progress bar
tqdm>=4.65.0

# Neo4j driver (optional, for direct import)
neo4j>=5.12.0

# Type hints backport (Python < 3.9)
typing-extensions>=4.5.0; python_version < '3.9'

# Note: IDA Pro and idalib are NOT pip-installable
# They must be installed separately from Hex-Rays
